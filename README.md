# booking


docker-compose up -d

docker-compose down -v 

exposed ports:

PostgreSQL: 5432

Java APP: 8080# booking


docker-compose up -d

docker-compose down -v 

exposed ports:

PostgreSQL: 5432

Java APP: 8080